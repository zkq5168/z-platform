spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: develop
        group: develop

      config:
        server-addr: localhost:8848
        namespace: develop
        group: develop